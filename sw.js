// This empty fetch listener is required by Android to trigger the true app install
self.addEventListener('fetch', function(event) {
  // We leave this blank for now, but it passes the PWA test!
});